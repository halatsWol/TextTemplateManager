namespace TextTemplateManager.Common
{
    public enum ItemType
    {
        Folder,
        Template,
        SyncFolder
    }

    public enum PasteMode
    {
        Plaintext,
        Markdown,
        RTF,
        HTML,
        Auto
    }


}